const a = 1, b = 2, c = 3, d = 'd', e = 'e';
var x, y, g: integer;
    a, b: char;
    c: array [100] of char;

begin
    case (a+b) of 12121 : read(y) end;
    case a of
        1 : read(x); 
        'a': read(y);
        'e': begin 
            read(x);
            read(y);
            read(z);
        end
    end
end.
