const v = 'a', c = 10;
var k, d: array [10] of integer;

function f(var a, b,c: char; i, j, k: integer): char;
const vv = 100, d = 'c';
var t: array [1] of char;
begin
    f := 100;
    for i := d to cc do
    begin
        f := f +1 ;
    end;
    if vv = d then write("eq");
    if vv < d then write("gt");
    if vv > d then write("lt");
    if vv <> d then write("ne");
    if vv >= d then write("ge");
    if vv <= d then write("le");
end;

begin
    for i := 1 to 10 do begin
        begin
            begin
                begin
                    begin
                        begin
                            begin
                                read(k[i]);
                            end
                        end
                    end;
                    begin
                        begin
                            a := 1 + 2;
                        end;
                    end;
                end
            end
        end
    end;
    write(f(k[1], k[2], k[3], k[4], k[5], k[6]));
    write(f(1, 2, 3));    
end.
