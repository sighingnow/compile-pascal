var a, b, c, d, e, f: integer;

procedure pn(var x, y: integer; z: integer);
begin
    x := 1 + 1;
    y := 1 + 1;
    z := 1;
end;

begin
    write("dddddddddddddd");
    read(b);
    a := (b + c) + (b +f);
    d := (b + c) + (b +f);
    e := (b + c) + (b +f);
end.
